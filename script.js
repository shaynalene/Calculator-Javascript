function add(a, b) {
  return a + b;
}
function subtract(a, b) {
  return a - b;
}
function multiply(a, b) {
  return a * b;
}
function divide(a, b) {
  return a / b;
}

/*
let calc = new Calculator;

function Calculator(){
    this.operations ={
        "+" = (a,b) => a+b,
        "-" = (a,b) => a-b,
        "*" = (a,b) => a*b,
        "/" = (a,b) => a/b,
    }
}
*/
